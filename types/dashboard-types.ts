export interface GeneratedImage {
  id: string;
  url: string;
  timestamp: Date;
  prompt: string;
}

export interface GeneratedVideo {
  id: string;
  url: string;
  timestamp: Date;
  prompt: string;
}

